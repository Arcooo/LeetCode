Data Structures

stack
linked list 
array
hashmap - you take the value of text e.g. a = 97 in ascii. Then you could add up all the characters into some number and mod % it into e.g. 10 buckets or hashes. You can have key-val pairs in each buckets if their are duplicates. Get the key-val fast bc the has function should be fast.

binary tree
queue
graph
window
pointers
node
tries
heap/priority queue
intervals
greedy




Algorithms
